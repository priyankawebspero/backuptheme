{% if customer %}
                                    {% assign customerId = customer.id %}
                                    {% assign customerName = customer.name %}
                                    {% assign customerEmail = customer.email %}
                                {% endif %}
                                <script async>AMSWINConfig = {user : {name : "{{ customerName }}", email : "{{ customerEmail }}", id : "{{ customerId }}", country : ""}, site : {id : "61662", domain : "plumgoodness-2.myshopify.com", platform : "sw_shopify"}};</script>
                                